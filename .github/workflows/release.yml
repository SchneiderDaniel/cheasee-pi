name: Release

on:
  pull_request:
    paths:
      - "cmd/cheasee-pi/**"
      - "go.mod"
      - "go.sum"
      - ".goreleaser.yml"
      - ".github/workflows/release.yml"
  push:
    tags:
      - "v*"

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25"
      - name: Run tests
        run: go test ./cmd/cheasee-pi/...
      - name: Assert single normal repo (no submodules)
        run: make check-no-submodules
      - name: Build smoke test
        run: go build ./cmd/cheasee-pi/
      - name: Validate GoReleaser config
        uses: goreleaser/goreleaser-action@v7
        with:
          args: check

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: test
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25"
      - uses: goreleaser/goreleaser-action@v7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: release --clean
