ddgs>=0.8
# Pin idna to the post-CVE-2026-45409 release (PYSEC-2026-215): older ddgs
# resolutions pulled requests → idna, and idna<3.15 is DoS-vulnerable on
# crafted long inputs. The pin guarantees a patched idna regardless of how
# the ddgs dependency tree resolves.
idna>=3.15
# Pin anyio (transitive: ddgs → httpx → anyio) to the release that fixes
# GHSA-82r6-8w77-94w6 (CVE-2026-63374, TLSStream IDNA-2003 spoofing) and
# GHSA-5p39-cfhj-2xmp (CVE-2026-64847, process-pool workers blocking on
# undrained stderr). Both are fixed in anyio 4.14.2.
anyio>=4.14.2
